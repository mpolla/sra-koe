# Simuloi kehityskäytössä GH pages -asennusta
# https://mpolla.github.io/sra-ampumakoe-laskuri/
services:
  proxy:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    network_mode: "host"
